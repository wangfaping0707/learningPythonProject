# #Python支持一种数据结构，名为容器，容器基本上就是可包含其他对象的对象。两种主要的容器是序列(列表和元组)
# 和映射(字典)。在序列中，每个元素都有编号，而映射中，每个元素都有名称(也叫键)，有一种既不是序列也不是映射的容
# 器,它就是集合(set)
edward = ["Edward Gumby", "42"]
john = ['John Smith', 50]
database = [edward, john]
print(database)
greeting='Hello'
print(greeting[-4:])
print("I love you"[4])
# 如果函数调用返回一个序列，可直接对其执行索引操作
print(input('Year:')[3])
