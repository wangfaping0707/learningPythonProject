"""
二、可变长度的关键字参数：

**形参名：用来接收溢出的关键字实参，**会将溢出的关键字实参保存成字典格式，然后赋值给紧跟其后的参数名
   **后可以是任意的名字，但约定俗成是kwarge
"""


def func(x, y, **kwargs):
	print(x, y, kwargs)


func(1, 3, a=4, b=5, c=7, d=9, e=87)

print('============================================================================》')
